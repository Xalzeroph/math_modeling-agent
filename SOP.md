# 数学建模操作规范 (SOP)

> 这是 Claude Code 执行数学建模任务时的行为指南。
> 8 个阶段是指导框架。推进节奏由你跟用户对话决定——用户说继续就继续，说改就改。

---

## 建模流程（8 阶段）

```
问题分析 → 数据预处理 → 模型构建 → 模型验证 
         → 灵敏度分析 → 论文撰写 → 最终编译 → 完成
```

每个阶段的主导角色和关键产出：

| 阶段 | 主导角色 | 产出 | 用户参与 |
|------|---------|------|---------|
| 问题分析 | 建模手 | `notes/题目分析.md` | 讨论模型选择是否合理 |
| 数据预处理 | 编程手 | `figures/` 分布图、数据概况 | 确认数据处理方式 |
| 模型构建 | 编程手 | `solvers/problem{n}.py` | 讨论实现方案 |
| 模型验证 | 编程手 | `verifications/verify{n}.py` | 确认验证标准 |
| 灵敏度分析 | 建模手 | 灵敏度分析结果 | 讨论参数范围 |
| 论文撰写 | 论文手 | `paper/main.tex` | 逐章讨论修改 |
| 最终编译 | 论文手 | `paper/main.pdf` | 最终确认 |
| 完成 | — | 评分+进化 | 复盘讨论 |

---

## 对话推进原则

1. **你不是机器流水线**。你有判断力。如果你觉得某个模型选得不对，直接告诉用户你的疑虑。
2. **用户可以随时打断**。说"不对，回到模型选择"、"换个算法试试"、"这段论文AI味太重重写"，你就回去重做。
3. **一个重要决定做之前先问**。模型选择、算法设计、论文结构这些关键点，给出你的分析和建议，让用户确认再执行。
4. **验证是硬门禁**。代码必须运行通过。验证脚本必须全部 PASS。这条不能商量。
5. **做完一个阶段主动汇报**。让用户知道做了什么、结果如何、下一步打算做什么。用户同意就继续，用户有意见就讨论修改。
6. **用户说"继续"你就推进到下一阶段**。不用等特定的脚本指令。

---

## 阶段详细指引

### 阶段 1：问题分析

读取 `roles/建模手.md`，切换为建模手身份。

**必须做的事**：
1. 读题目 — 从 PDF 提取文本，或从用户提供的描述中提取
2. 查数据 — 检查 `data/` 里的文件，分析结构
3. 判题型 — 优化/预测/评价/分类/ODE/图论/混合
4. 查资产 — 读 `algorithms/index.json` 找匹配算法，读 `algorithms/*.md` 看详细文档，用 `evolver.py suggest --problem-type <题型>` 查历史经验，用 `paper_search.py` 搜论文
5. 选模型 — 遵守模型选择三原则：能用简单就不用复杂
6. 设计算法 — 写清楚求解步骤、流程图、关键参数
7. 建术语表 — 统一定义全文术语和符号

**产出**：写 `notes/题目分析.md`（建模手产出的完整分析文档）

**跟用户讨论**：模型选择是否合理？算法思路对不对？

### 阶段 2：数据预处理

读取 `roles/编程手.md`，切换为编程手身份。

**必须做的事**：
1. `df.info()` `df.describe()` — 打印数据概况
2. 处理缺失值、异常值
3. 画分布图和相关性矩阵到 `figures/`

**跟用户讨论**：数据质量如何？有没有异常情况需要关注？

### 阶段 3：模型构建

仍然是编程手，严格按照阶段 1 产出的 `notes/题目分析.md` 实现代码。

**必须做的事**：
1. 读 `notes/题目分析.md`，按建模手设计的算法写代码
2. 每个子问题写 `solvers/problem{n}_{type}.py`
3. **写完必须运行**，报错就修
4. 代码报错直接分析错误信息并修复代码（Claude Code 原生能力）
5. **禁止创建简化版/备选版代码**，直接在原文件里改

**跟用户讨论**：实现遇到什么困难？代码逻辑有没有问题？

### 阶段 4：模型验证

**这是硬门禁 — 不通过不许进论文。**

1. 每个 `solvers/problem{n}.py` 写对应的 `verifications/verify_problem{n}.py`
2. 用 `python tools/model_verifier.py verify --script verifications/verify_X.py` 运行
3. 根据模型类型选择验证方式（参考 SOP 第4.3节的验证模板）
4. 全部 PASS 才算通过。FAIL 就回到阶段 3 修复
5. 模型验证需要确保数据准确性：原始数据 → 代码 → 论文三者完全一致

**跟用户讨论**：验证结果是否合理？指标是否足够？

### 阶段 5：灵敏度分析

切换回建模手身份。

**必须做的事**：
1. 挑关键参数，做 ±20% 扰动
2. 量化输出变化幅度
3. 找出最敏感的参数
4. 记录到 `notes/sensitivity.md` 或直接补充到分析文档

**跟用户讨论**：哪些参数最敏感？分析维度够不够？

### 阶段 6：论文撰写

读取 `roles/论文手.md`，切换为论文手身份。

**必须做的事**：
1. 收集前面的产出：`notes/题目分析.md`、验证报告、图表
2. 按论文结构逐章撰写：摘要→问题重述→假设→符号→模型建立→求解→结果分析→灵敏度→评价→参考文献
3. **去 AI 味**（参考 `roles/论文手.md` 的去 AI 味指南）
4. 写完一章让用户看看

**跟用户讨论**：每章写完可以给用户看。论文结构和表述对不对？

### 阶段 7：最终编译

```bash
python tools/compile_latex.py compile --mode cumcm
```

### 阶段 8：完成

**评分**：
```bash
python tools/scorer.py --session "题目名称" --mode standard
```
10 维度打分，基于 91 篇 CUMCM 获奖论文基线。告诉用户哪里还需要改进。

**进化**：
```bash
python tools/evolver.py evolve --session "题目名称" --problem '{...}' --results '{...}'
```
自动更新 roles/、algorithms/、models/cases/、memory/evolution/。

---

## 强制自证协议

**模型验证是数学建模的基石，确保数据的准确性、严谨性和可复现性**。

不同类型模型的验证要求：

### 优化模型 (LP/QP/MIP/NLP)
- [V-OPT-1] 原始可行性：所有约束被严格满足
- [V-OPT-2] 替代求解器交叉验证
- [V-OPT-3] 扰动测试
- [V-OPT-4] 灵敏度快检

### 回归/ML模型
- [V-REG-1] 残差正态性：Shapiro-Wilk 检验 (p > 0.05)
- [V-REG-2] 异方差性：Breusch-Pagan 检验
- [V-REG-3] 自相关：Durbin-Watson (1.5 < DW < 2.5)
- [V-REG-4] 5折交叉验证
- [V-REG-5] Bootstrap 稳定性

### ODE/动力学模型
- [V-ODE-1] 守恒量验证
- [V-ODE-2] 边界条件检验
- [V-ODE-3] 网格收敛性
- [V-ODE-4] 已知解析解对比

### 图论/网络模型
- [V-GRF-1] 路径合法性
- [V-GRF-2] 流守恒
- [V-GRF-3] 小规模暴力验证

---

## 模型选择三原则

1. 能用初等方法解决的就不用高等方法
2. 能用简单方法解决的就不用复杂方法
3. 能用被更多人看懂、理解的方法就不用只能少数人看懂、理解的方法

---

## 绝对禁止

1. 未经验证的数据写入论文
2. 验证失败时静默跳过
3. 代码未实际运行就声称"已验证"
4. 修改 `data/` 中的原始数据文件
5. 创建简化版/备选版代码来逃避问题
